# Pure-python extras (installable with pip).
beautifulsoup4
gdown
imageio
imageio-ffmpeg
mediamtx-py
opencv-python
pokemontcgsdk
PySide6
PyYAML
tqdm
matplotlib
requests
numpy>=1.26,<3

# --- The following are installed manually, NOT via this file (see README.md) ---
# torch==2.12.1+cu132 / torchvision  (CUDA 13.2 wheels, RTX 5080 / Blackwell)
# mmengine                          (pip install mmengine)
# mmcv>=2.0, mmdet>=3.0, mmrotate>=1.0  (via `mim install ...`)
