"""PTCG-AR shared core library (config, image ops, transforms, training utils,
databases, shared memory and the model wrappers).  Imported by the installation,
training, evaluation and inference packages so that no code is duplicated."""
